"""
Django settings for gaboshop project.

Generated by 'django-admin startproject' using Django 5.2.8.

For more information on this file, see
https://docs.djangoproject.com/en/5.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/5.2/ref/settings/
"""

from pathlib import Path
from datetime import timedelta
import os

# small helper to read environment variables in settings where a full
# django-environ or decouple isn't configured. Use real env library in prod.
def env(name, default=None):
    return os.environ.get(name, default)

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/5.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'django-insecure-(v504-88060g^)_%d!8n(6w$g^znk2d(9ezkp024t$hu(if5fu'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

# Autoriser les requets locales (frontend React et API en dveloppement)
ALLOWED_HOSTS = ['localhost', '127.0.0.1']


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    # Third-party
    'rest_framework',
    'corsheaders',
    'django_celery_beat',
    'django_celery_results',

    # Local apps
    'api',
    'users',
    'stores',
    'products',
    'orders',
    'payments',
    'delivery',
    'notifications',
    'revenue',
    'core',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'corsheaders.middleware.CorsMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'gaboshop.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'gaboshop.wsgi.application'


# Database
# https://docs.djangoproject.com/en/5.2/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}


# Password validation
# https://docs.djangoproject.com/en/5.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/5.2/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/5.2/howto/static-files/

STATIC_URL = 'static/'

# Default primary key field type
# https://docs.djangoproject.com/en/5.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Django REST Framework
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': (
        'rest_framework_simplejwt.authentication.JWTAuthentication',
    ),
    'DEFAULT_PERMISSION_CLASSES': (
        'rest_framework.permissions.IsAuthenticated',
    ),
}

# Simple JWT settings (adjust lifetimes as needed)
SIMPLE_JWT = {
    'ACCESS_TOKEN_LIFETIME': timedelta(minutes=60),
    'REFRESH_TOKEN_LIFETIME': timedelta(days=7),
    'AUTH_HEADER_TYPES': ('Bearer',),
}

# CORS
# In development you can allow all origins. Lock this down for production.
CORS_ALLOW_ALL_ORIGINS = True
# Allow cookies (Authorization headers are allowed by default for JWT)
CORS_ALLOW_CREDENTIALS = True

# Example: whitelist specific origins in production instead of allowing all
# CORS_ALLOWED_ORIGINS = [
#     'https://example.com',
#     'https://sub.example.com',
# ]

# Custom user model
AUTH_USER_MODEL = 'users.User'

# === DRF CONFIGURATION ===
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': (
        'rest_framework_simplejwt.authentication.JWTAuthentication',
    ),
    'DEFAULT_PERMISSION_CLASSES': (
        'rest_framework.permissions.IsAuthenticated',
    ),
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 20,
    'DEFAULT_RENDERER_CLASSES': (
        'rest_framework.renderers.JSONRenderer',
    ),
    'EXCEPTION_HANDLER': 'api.v1.exceptions.custom_exception_handler',
}

# === JWT CONFIGURATION ===
SIMPLE_JWT = {
    'ACCESS_TOKEN_LIFETIME': timedelta(days=1),
    'REFRESH_TOKEN_LIFETIME': timedelta(days=7),
    'ROTATE_REFRESH_TOKENS': True,
    'BLACKLIST_AFTER_ROTATION': True,
    'AUTH_HEADER_TYPES': ('Bearer',),
    'AUTH_TOKEN_CLASSES': ('rest_framework_simplejwt.tokens.AccessToken',),
}

# CORS Configuration - use explicit origins rather than allow-all in production
CORS_ALLOW_ALL_ORIGINS = False
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
    "http://127.0.0.1:3000",
    "http://localhost:5173",
    "http://127.0.0.1:5173",
]

CSRF_TRUSTED_ORIGINS = [
    "http://localhost:8000",
    "http://127.0.0.1:8000",
    "http://localhost:5173",
    "http://127.0.0.1:5173",
]

# === FILE UPLOADS ===
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'

# Maximum file size for uploads (10MB)
DATA_UPLOAD_MAX_MEMORY_SIZE = 10485760
FILE_UPLOAD_MAX_MEMORY_SIZE = 10485760

# Mobile Money APIs Configuration (Gabon)
AIRTEL_API_KEY = env('AIRTEL_API_KEY', default='')
AIRTEL_MERCHANT_ID = env('AIRTEL_MERCHANT_ID', default='')
AIRTEL_API_SECRET = env('AIRTEL_API_SECRET', default='')

MOOV_API_KEY = env('MOOV_API_KEY', default='')
MOOV_MERCHANT_ID = env('MOOV_MERCHANT_ID', default='')
MOOV_API_SECRET = env('MOOV_API_SECRET', default='')

# WhatsApp Business API
WHATSAPP_TOKEN = env('WHATSAPP_TOKEN', default='')
WHATSAPP_PHONE_ID = env('WHATSAPP_PHONE_ID', default='')

# Notifications / Messaging configuration
WHATSAPP_BUSINESS_API_URL = env('WHATSAPP_BUSINESS_API_URL', default='https://graph.facebook.com/v17.0')
WHATSAPP_PHONE_ID = env('WHATSAPP_PHONE_ID', default='')
WHATSAPP_ACCESS_TOKEN = env('WHATSAPP_ACCESS_TOKEN', default='')
WHATSAPP_VERIFY_TOKEN = env('WHATSAPP_VERIFY_TOKEN', default='gaboshop_webhook_2024')

# SMS Configuration
SMS_PROVIDER = env('SMS_PROVIDER', default='twilio')  # twilio, infobip, hub2
TWILIO_ACCOUNT_SID = env('TWILIO_ACCOUNT_SID', default='')
TWILIO_AUTH_TOKEN = env('TWILIO_AUTH_TOKEN', default='')
TWILIO_PHONE_NUMBER = env('TWILIO_PHONE_NUMBER', default='')

# Hub2SMS Gabon Configuration
HUB2SMS_API_KEY = env('HUB2SMS_API_KEY', default='')
HUB2SMS_SENDER_ID = env('HUB2SMS_SENDER_ID', default='GABOSHOP')

# Email Configuration (optionnel)
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = env('EMAIL_HOST', default='smtp.gmail.com')
EMAIL_PORT = env('EMAIL_PORT', default=587)
EMAIL_USE_TLS = env('EMAIL_USE_TLS', default=True)
EMAIL_HOST_USER = env('EMAIL_HOST_USER', default='')
EMAIL_HOST_PASSWORD = env('EMAIL_HOST_PASSWORD', default='')
DEFAULT_FROM_EMAIL = env('DEFAULT_FROM_EMAIL', default='GABOSHOP <noreply@gaboshop.ga>')

# Templates de notifications
NOTIFICATION_TEMPLATES = {
    'new_order': {
        'whatsapp': 'nouvelle_commande',
        'sms': 'Nouvelle commande #{order_number} - {total_amount} FCFA - {client_phone}',
        'email': 'notifications/new_order.html'
    },
    'order_status_update': {
        'whatsapp': 'statut_commande', 
        'sms': 'Commande #{order_number}: {status}',
        'email': 'notifications/order_status.html'
    },
    'delivery_assigned': {
        'whatsapp': 'livraison_assignee',
        'sms': 'Nouvelle livraison #{tracking_number} - {delivery_fee} FCFA',
        'email': 'notifications/delivery_assigned.html'
    },
    'payment_success': {
        'whatsapp': 'paiement_confirme',
        'sms': 'Paiement confirmé - Commande #{order_number} - {amount} FCFA',
        'email': 'notifications/payment_success.html'
    },
    'payment_failed': {
        'whatsapp': 'paiement_echec',
        'sms': 'Paiement échoué - Commande #{order_number}',
        'email': 'notifications/payment_failed.html'
    }
}

# === CELERY CONFIGURATION ===
CELERY_BROKER_URL = env('REDIS_URL', default='redis://localhost:6379/0')
CELERY_RESULT_BACKEND = 'django-db'
CELERY_ACCEPT_CONTENT = ['json']
CELERY_TASK_SERIALIZER = 'json'
CELERY_RESULT_SERIALIZER = 'json'
CELERY_TIMEZONE = 'Africa/Libreville'
CELERY_ENABLE_UTC = True

# Beat Schedule (tâches périodiques)
CELERY_BEAT_SCHEDULE = {
    'assigner-livreurs-toutes-les-5-min': {
        'task': 'orders.tasks.assigner_livreurs_automatique',
        'schedule': 300.0,  # 5 minutes
    },
    'nettoyer-paniers-abandonnes-toutes-les-heures': {
        'task': 'orders.tasks.nettoyer_paniers_abandonnes',
        'schedule': 3600.0,  # 1 heure
    },
    'verifier-paiements-en-attente-toutes-les-2-min': {
        'task': 'payments.tasks.verifier_paiements_en_attente',
        'schedule': 120.0,  # 2 minutes
    },
    'traiter-reversements-quotidien': {
        'task': 'payments.tasks.traiter_reversements_automatiques',
        'schedule': 86400.0,  # 24 heures
    },
    'envoyer-rapports-quotidien': {
        'task': 'notifications.tasks.envoyer_rapports_quotidiens',
        'schedule': 86400.0,  # 24 heures à 8h00
    },
}

# Cache Configuration
CACHES = {
    'default': {
        'BACKEND': 'django_redis.cache.RedisCache',
        'LOCATION': env('REDIS_URL', default='redis://localhost:6379/1'),
        'OPTIONS': {
            'CLIENT_CLASS': 'django_redis.client.DefaultClient',
        }
    }
}

# ============================================================================
# CINETPAY / AIRTEL MONEY / MOOV MONEY CONFIGURATION
# ============================================================================

# CinetPay Configuration
CINETPAY_API_URL = "https://api-checkout.cinetpay.com/v2/payment"
CINETPAY_CHECK_URL = "https://api-checkout.cinetpay.com/v2/payment/check"
CINETPAY_REFUND_URL = "https://api-checkout.cinetpay.com/v2/refund"
CINETPAY_SITE_ID = env("CINETPAY_SITE_ID", default="")
CINETPAY_APIKEY = env("CINETPAY_APIKEY", default="")
CINETPAY_SECRET = env("CINETPAY_SECRET", default="")
CINETPAY_NOTIFY_URL = env("CINETPAY_NOTIFY_URL", default="http://localhost:8000/api/v1/payments/provider/cinetpay/notify/")
CINETPAY_RETURN_URL = env("CINETPAY_RETURN_URL", default="http://localhost:3000/checkout/success")

# Airtel Money Configuration
AIRTEL_MERCHANT_ID = env("AIRTEL_MERCHANT_ID", default="")
AIRTEL_API_KEY = env("AIRTEL_API_KEY", default="")
AIRTEL_USSD_API_URL = "https://api.airtel.ga/v2/payment/request"
AIRTEL_CHECK_URL = "https://api.airtel.ga/v2/payment/status"

# Moov Money Configuration
MOOV_MERCHANT_ID = env("MOOV_MERCHANT_ID", default="")
MOOV_API_KEY = env("MOOV_API_KEY", default="")
MOOV_API_URL = "https://api.moov.ga/v1/payment/request"
MOOV_CHECK_URL = "https://api.moov.ga/v1/payment/status"
